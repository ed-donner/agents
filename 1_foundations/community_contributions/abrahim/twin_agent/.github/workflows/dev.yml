name: Build and Push to GHCR

on:
  push:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner  }}/digital-twin

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Set Render environment variables
        run: |
          curl -s -X PUT "https://api.render.com/v1/services/${{ secrets.RENDER_SERVICE_ID }}/env-vars" \
            -H "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d '[
              {"key": "OPENROUTER_ENDPOINT", "value": "${{ secrets.OPENROUTER_ENDPOINT }}"},
              {"key": "OPENROUTER_API_KEY", "value": "${{ secrets.OPENROUTER_API_KEY }}"},
              {"key": "GROQ_ENDPOINT", "value": "${{ secrets.GROQ_ENDPOINT }}"},
              {"key": "GROQ_API_KEY", "value": "${{ secrets.GROQ_API_KEY }}"},
              {"key": "AZURE_FOUNDRY_ENDPOINT", "value": "${{ secrets.AZURE_FOUNDRY_ENDPOINT }}"},
              {"key": "AZURE_FOUNDRY_API_KEY", "value": "${{ secrets.AZURE_FOUNDRY_API_KEY }}"},
              {"key": "PUSHOVER_USER", "value": "${{ secrets.PUSHOVER_USER }}"},
              {"key": "PUSHOVER_TOKEN", "value": "${{ secrets.PUSHOVER_TOKEN }}"}
            ]'

      - name: Trigger Render deploy
        run: curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK }}"